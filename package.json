{
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "name": "teamflow-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\""
  }
}
